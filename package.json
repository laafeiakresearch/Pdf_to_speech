{
  "name": "ai-text-reader-static",
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build index.html --public-url ./",
    "dev": "parcel index.html",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel": "^2.10.0",
    "tailwindcss": "^3.4.0",
    "gh-pages": "^6.1.1"
  }
}
